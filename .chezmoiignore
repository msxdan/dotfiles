.key.age
README.md

{{/* macOS-only targets */}}
{{- if ne .chezmoi.os "darwin" }}
Library
ibooks
.brewfile
{{- end }}

{{/* Linux-only targets */}}
{{- if ne .chezmoi.os "linux" }}
.config/systemd
{{- end }}

{{/* Graphical-desktop-only targets */}}
{{- if not (and (eq .chezmoi.os "linux") .isDesktop) }}
.config/solaar
{{- end }}

{{/* KDE-only targets */}}
{{- if not .isKDE }}
.config/systemd/user/app-org.kde.krdpserver.service.d
{{- end }}

{{/* Work-only: credentials and contexts that belong to the job, not the machine */}}
{{- if not .isWork }}
.pgpass
.pg_service.conf
.databrickscfg
.datagrip/ec
.config/infracost
.ssh/config.d/mm
{{- end }}

{{/* Personal-only */}}
{{- if not .isPersonal }}
.datagrip/marvin-cloud
{{- end }}

{{/* Kubeconfigs, selected at init */}}
{{- if not (has "homelab" .kubeClusters) }}
.kube/config.d/homelab
{{- end }}
{{- if not (has "ec" .kubeClusters) }}
.kube/config.d/ec
{{- end }}
{{- if not (has "mm" .kubeClusters) }}
.kube/config.d/mm
{{- end }}
